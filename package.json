{
  "name": "obsidian-phrase-linker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild main.ts --bundle --external:obsidian --platform=node --format=cjs --outfile=main.js",
    "dev": "npm run build -- --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "esbuild": "^0.21.5",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "obsidian": "latest"
  }
}
